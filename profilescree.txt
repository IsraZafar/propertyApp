main.dart

import 'package:flutter/material.dart';
import 'package:profilescreen/profilescreen.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Profile Screen',
      theme: ThemeData(
        primarySwatch: Colors.deepOrange,
      ),
      home: const Profilescreen(),
    );
  }
}

profilescreen.dart

import 'dart:js_interop';

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class Profilescreen extends StatelessWidget {
  const Profilescreen({super.key});

  child() => null;

  @override
  Widget build(BuildContext context) {
    var decoration;
    var children = <Widget>[
          CircleAvatar(
            radius: 50,
            backgroundImage: AssetImage("images/profileimage.png"),
    
          ),
    ];
      
        
      

          const SizedBox(height: 20);
        itemprofile ("Name","IsraZafar",CupertinoIcons.person);
          const SizedBox(height:10);
       itemprofile("Billing","Billing",CupertinoIcons.clock);
        const SizedBox(height:10);
          itemprofile("Order","Order",CupertinoIcons.book);
         const SizedBox(height:10);
         itemprofile("Faq","Faq",CupertinoIcons.person );
         
         ElevatedButton(onPressed: (){}, 
             style:ElevatedButton.styleFrom(
              padding: EdgeInsets.all(15),
             ),
         child: const Text("Edit Profile"));
       
        


            decoration;
    return Scaffold(
      body: Column(
        children: children:BoxDecoration(
             color: Colors.blue,
                borderRadius: BorderRadius.circular(10),
            boxShadow: [
              BoxShadow(
                     offset: Offset(0, 5),
                     color: Colors.deepPurpleAccent.withOpacity(.2),
                      spreadRadius: 5,
                     blurRadius: 10,
                  ),
        ],
                                                                                                       
      ),
      
      
        
             child: ListTile(
            title: const Text("title"),
                subtitle: const Text("subtitle"),
               leading: Icon(IconData),
              trailing: Icon(Icons.arrow_back, color: Colors.blue),
                      ),
    ),
     );
          
  }
}

        
        


// itemprofile(String s, IconData book) {
//   return Container();
// }
itemprofile(String Title ,String subtitle, IconData iconData){
  return Container(
        decoration: BoxDecoration(
             color: Colors.blue,
                borderRadius: BorderRadius.circular(10),
            boxShadow: [
              BoxShadow(
                     offset: Offset(0, 5),
                     color: Colors.deepPurpleAccent.withOpacity(.2),
                      spreadRadius: 5,
                     blurRadius: 10,
                   ),
              ]),
             child: ListTile(
            title: const Text("title"),
                subtitle: const Text("subtitle"),
               leading: Icon(IconData as IconData?),
              trailing: Icon(Icons.arrow_back, color: Colors.blue),
              ),
  
  );

}

  